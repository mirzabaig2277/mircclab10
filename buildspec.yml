version: 0.2

phases:
  install:
    commands:
      - echo Installing...

  build:
    commands:
      - echo Build started
      - echo No build needed for static website

artifacts:
  files:
    - '**/*'
